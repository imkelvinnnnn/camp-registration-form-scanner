{
  "name": "internal-registration-form-scanner",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "Browser-only fixed-template registration form scanner for GitHub Pages.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check js/*.js && node --check config/*.js"
  }
}
